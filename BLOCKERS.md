# Blockers — 新一轮 SFT 严谨性实验

只放"必须用户决策才能继续"的事项。空 = 当前无阻塞。

## Open

1. **`internal_seed_flywheel`（真实业务种子数据，14,718 条）的脱敏状态未知——且已有
   具体证据表明可能不完整。** 读了 `scripts_cloud/sft_data/clean_02.py` 之后发现：
   现有清洗流程**确实有** PII 正则检测（手机号/QQ/邮箱/座机号，`clean_02.py` 第22行
   `PII` 正则），但对种子业务数据的处理策略是"**只标记 quality_flags，不删除不替换**"
   （第85-87行：`protect = (src == "internal_seed_flywheel")` 时命中 PII 规则不丢弃，
   只打标签）；而且这个 PII 正则只覆盖电话/QQ/邮箱，**不覆盖**姓名、身份证号、住址、
   病历号、精确就诊时间——`spec/requirements_sft_rigor.md` §11 要求的这些字段目前
   完全没有检测机制。也就是说：即使这份数据里没有命中现有 PII 正则的电话/QQ/邮箱，
   姓名/身份证/住址/病历号这类信息如果存在，现在的流程**不会**发现也不会处理。
   在得到你明确答复前，我会把这份数据当作**未完成脱敏**处理：不会把其具体内容打印到
   日志、不会写入任何会进 git 的文件、不会包含在报告示例里。
   **需要你回答**：这份数据源头（业务飞轮）在进入这个 kit 之前是否已经做过脱敏？
   如果没有，是否需要我先扩展脱敏规则（覆盖姓名/身份证/住址/病历号/精确时间）并跑一版
   脱敏，再进入新一轮的清洗/去重/contamination 检查流程？
2. **是否已确认没有其他"必须先问清楚才能继续"的科研结论/许可问题。** 本轮工作大部分
   是数据审计和查漏补缺（复用现有已清洗数据），暂未发现需要停下来问的科学结论性问题；
   若在补全 §7/§9/§26/§33 等审计脚本时发现数据量/质量的意外情况（例如 contamination
   检查发现训练数据确实包含 benchmark 题目），会在这里补充记录，不会自行删除或悄悄处理。

## Resolved

- **2026-07-19 — 项目归属 + 本轮算力。** 用户决定：延续 `rlhf_lab_cloud_kit`
  （不新建项目、不迁移到 cmedalign/OpenRLHF）；本机 RTX 3060 Ti（8GB）现在就用于
  纯数据/CPU 级别准备工作，真正的 LoRA 训练等公司"5133"机器或新租服务器。见
  `spec/design.md` 决策记录。
